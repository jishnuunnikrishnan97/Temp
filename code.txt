# Assuming 'table' is your DataFrame
columns_to_multiply = [col for col in table.columns if 'usd' in col.lower()]
table[columns_to_multiply] = table[columns_to_multiply].replace({',': ''}, regex=True).astype(float) * 8.25
