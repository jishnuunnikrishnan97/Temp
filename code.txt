import pandas as pd

# Assuming your DataFrame is named "data_frame"

# Create a new DataFrame "Manual" with rows where 'Customer Id' is "Check Manually"
manual_df = data_frame[data_frame['Customer Id'] == 'Check Manually']

# Create a new DataFrame "Partial" with rows where 'Customer Id' is NaN
partial_df = data_frame[data_frame['Customer Id'].isnull()]

# Remove the rows from the original DataFrame "data_frame"
data_frame = data_frame.drop(manual_df.index)
data_frame = data_frame.drop(partial_df.index)

# Print or use the new DataFrames as needed
print("Manual DataFrame:")
print(manual_df)

print("\nPartial DataFrame:")
print(partial_df)

print("\nRemaining DataFrame:")
print(data_frame)
