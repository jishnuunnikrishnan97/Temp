def extract_text_without_footer(pdf_path, regex_pattern):
    text = ""
    doc = fitz.open(pdf_path)
    
    for page in doc:
        page_text = page.get_text("text")
        matches = re.search(regex_pattern, page_text)
        
        if matches:
            # Use the part of the text before the matched pattern
            text += page_text[:matches.start()]
        else:
            text += page_text
    
    return text