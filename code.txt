# Assuming you have a DataFrame named 'df'
if 'Amount(USD Million)' in df.columns:
    df.loc[df['Amount(USD Million)'].notna(), 'Amount(USD Million)'] = df.loc[df['Amount(USD Million)'].notna(), 'Amount(USD Million)'].str.replace(',', '', regex=True).astype(float) * 8.25
