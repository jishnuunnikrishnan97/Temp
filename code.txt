def check_and_extract_table(sheet_name):
    df = pd.read_excel('your_excel_file.xlsx', sheet_name=sheet_name)
    for string in stringlist:
        if any(df.applymap(lambda cell: string == str(cell))):
            return df
    return None
