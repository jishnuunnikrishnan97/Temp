list_index = -1  # Index of the list
element_index = -1  # Index of the element containing 'asdk'

# Iterate through the lists
for i, inner_list in enumerate(data):
    if 'asdk' in inner_list:
        list_index = i
        element_index = inner_list.index('asdk')
        break