import pandas as pd

# Assuming you have a DataFrame named 'df'.
# Replace 'df' with the actual name of your DataFrame.

# Use boolean indexing to set 'Rating' to 'ball' where 'facilities' contains 'long'.
df.loc[df['facilities'].str.contains('long'), 'Rating'] = 'ball'
