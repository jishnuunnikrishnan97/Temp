selected_list = None
index = None

# Iterate through the list_of_lists to find the selected list
for i, lst in enumerate(list_of_lists):
    if search_string in lst:
        selected_list = lst
        index = lst.index(search_string)
        break