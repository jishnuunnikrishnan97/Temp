tab_data.columns = tab_data.columns.to_series().groupby(tab_data.columns).cumcount().add(1).astype(str).radd('Rating').where(tab_data.columns.to_series().duplicated(keep='first', case=False), tab_data.columns)
