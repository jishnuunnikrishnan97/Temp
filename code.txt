for i, row in table_df.iterrows():
    # Find the corresponding 'Facility' in the 'data' DataFrame
    matching_row = data_df[data_df['Facility'] == row['Facility']]
    
    # Check if a match was found
    if not matching_row.empty:
        # Populate the 'Rating' column in the 'table' DataFrame
        table_df.at[i, 'Rating'] = matching_row['Rating'].values[0]