encountered_patterns = set()

for item in string_list:
    match = re.search(pattern, item)
    if match:
        matched_text = match.group(0)
        if matched_text not in encountered_patterns:
            # Include the first occurrence
            encountered_patterns.add(matched_text)
            result_list_of_lists.append([item])
        else:
            # Exclude the second occurrence
            result_list_of_lists[-1].append(item)