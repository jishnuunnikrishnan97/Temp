selected_list = [lst for lst in list_of_lists if any(string.contains(search_string) for string in lst)]

# Check if the selected list exists
if selected_list:
    # Get the index of the search_string in the selected list
    index = selected_list[0].index(search_string)