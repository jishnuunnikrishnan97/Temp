df = pd.DataFrame(data)

# Iterate through columns and drop the one without 'icra' using str.contains
for column in df.columns:
    if df[column].str.contains('icra', case=False, na=False).all():
        df.drop(column, axis=1, inplace=True)

print(df)